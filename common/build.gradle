apply from: "https://raw.githubusercontent.com/SteveKunG/GradleScripts/refs/heads/v2/architectury/common.gradle"

tasks.named("shadowJar") {
    enabled = false
}

dependencies {
    implementation("com.moulberry:mixinconstraints:${mixin_constraints_version}")
}